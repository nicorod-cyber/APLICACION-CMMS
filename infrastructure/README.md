# Compatibilidad de infraestructura

El contexto maestro inicial propuso `infrastructure/`. El prompt 01 define `infra/` como carpeta activa para Docker, Nginx y scripts.

Esta carpeta queda como punto de compatibilidad documental; los archivos operativos actuales estan en `infra/`.

