import { FaenaSelect } from "../../faenas/FaenaSelect";
export type UnitTransferValue = { faenaDestinoCodigo: string; fechaEfectivaUtc: string; motivo: string; observaciones: string };
export function UnitTransferForm({ value, onChange, disabled }: { value: UnitTransferValue; onChange: (value: UnitTransferValue) => void; disabled?: boolean }) { return <div className="space-y-3"><FaenaSelect label="Faena destino" value={value.faenaDestinoCodigo} onChange={faenaDestinoCodigo => onChange({ ...value, faenaDestinoCodigo })} includeInactive={false} searchable disabled={disabled} emptyLabel="Selecciona la faena destino" /><label className="block text-sm font-medium">Fecha efectiva<input aria-label="Fecha efectiva" className="input mt-1" required disabled={disabled} type="datetime-local" value={value.fechaEfectivaUtc} onChange={event => onChange({ ...value, fechaEfectivaUtc: event.target.value })} /></label><label className="block text-sm font-medium">Motivo<textarea aria-label="Motivo" className="input mt-1 min-h-20" required disabled={disabled} value={value.motivo} onChange={event => onChange({ ...value, motivo: event.target.value })} /></label><label className="block text-sm font-medium">Observaciones<textarea aria-label="Observaciones" className="input mt-1 min-h-16" disabled={disabled} value={value.observaciones} onChange={event => onChange({ ...value, observaciones: event.target.value })} /></label></div>; }
