import { ClipboardPlus, FilePlus2, Truck } from "lucide-react";
import { useNavigate } from "react-router-dom";
export function UnitMaintenanceActions({ unitCode, canTransfer = true }: { unitCode: string; canTransfer?: boolean }) { const navigate = useNavigate(); const query = "targetType=OperationalUnit&targetCode=" + encodeURIComponent(unitCode); return <div className="grid gap-2"><button className="secondary-button" type="button" onClick={() => navigate("/avisos?" + query)}><FilePlus2 className="h-4 w-4" />Crear aviso</button><button className="secondary-button" type="button" onClick={() => navigate("/ot?" + query)}><ClipboardPlus className="h-4 w-4" />Crear OT</button>{!canTransfer ? <p className="text-xs text-slate-500"><Truck className="mr-1 inline h-3 w-3" />No autorizado para trasladar.</p> : null}</div>; }
