# SharePoint simulado

Carpeta local usada por el adaptador `LocalSimulation` para documentos, evidencias, PDFs de alerta y respaldos de importacion.

`LocalSimulator` sigue aceptado como alias legacy. La metadata asociada vive en `data/excel/sharepoint_files.xlsx`.
